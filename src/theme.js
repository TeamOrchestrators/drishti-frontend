const colors = {
  bg: "var(--color-bg)",
  bgRaised: "var(--color-bg-raised)",
  panel: "var(--color-panel)",
  panelAlt: "var(--color-panel-alt)",
  border: "var(--color-border)",
  borderSoft: "var(--color-border-soft)",
  text: "var(--color-text)",
  textMuted: "var(--color-text-muted)",
  textFaint: "var(--color-text-faint)",
  ice: "var(--color-ice)",
  iceDim: "var(--color-ice-dim)",
  iceBg: "var(--color-ice-bg)",
  iceButtonText: "var(--color-ice-button-text)",
  aurora: "var(--color-aurora)",
  auroraDim: "var(--color-aurora-dim)",
  auroraBg: "var(--color-aurora-bg)",
  amber: "var(--color-amber)",
  amberDim: "var(--color-amber-dim)",
  amberBg: "var(--color-amber-bg)",
  flare: "var(--color-flare)",
  flareDim: "var(--color-flare-dim)",
  flareBg: "var(--color-flare-bg)",
  flareTint: "var(--color-flare-tint)",
  modalOverlay: "var(--color-modal-overlay)",
};

const font = {
  fontFamily: "sans-serif",
};

const mono = {
  fontFamily: "'JetBrains Mono', 'SF Mono', Menlo, monospace",
};

const toneColors = {
  ice: colors.ice,
  amber: colors.amber,
  flare: colors.flare,
  aurora: colors.aurora,
  muted: colors.textFaint,
};

export { colors, font, mono, toneColors };
